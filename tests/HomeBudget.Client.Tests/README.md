# HomeBudget.Client.Tests

Testy wspólnego klienta HTTP.

Docelowo powinny obejmować obsługę błędów 401, 403, 422 i 500, odświeżanie tokena, błędy sieci oraz retry tylko dla bezpiecznych operacji.

Na tym etapie projekt testowy pozostaje bez testów.
